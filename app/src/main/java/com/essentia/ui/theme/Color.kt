package com.essentia.ui.theme

import androidx.compose.ui.graphics.Color

// Editorial palette — off-white newspaper background, near-black text, single accent.
val PaperWhite = Color(0xFFFAF8F4)        // primary background — warm off-white
val InkBlack = Color(0xFF121212)          // primary text — near-black
val InkSoft = Color(0xFF3D3D3D)           // body text
val InkMuted = Color(0xFF7A7A7A)          // captions, timestamps
val Rule = Color(0xFFE4E0D8)              // hairline dividers

val Accent = Color(0xFFB8410F)            // burnt orange — single accent
val AccentSoft = Color(0xFFF1D9C8)        // tinted background for tags

// Category-specific accent (used sparingly)
val BreakingRed = Color(0xFFB8410F)       // same as Accent (red-ish, matches mockup)
val AnalysisBlue = Color(0xFF1F3A5F)
val OpinionViolet = Color(0xFF5A3A6E)

// Surface
val Surface = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFF1EDE5)

// Bottom nav
val NavBackground = Color(0xFFFAF8F4)
val NavIndicator = Color(0xFF121212)
val NavIconActive = Color(0xFF121212)
val NavIconInactive = Color(0xFFB0AEA8)
